"""Placeholder RAG service orchestrating retrieval + LLM."""

def run_rag(query: str, top_k: int = 5):
    # stub: returns the query echoed back with dummy context
    return {"query": query, "results": []}
